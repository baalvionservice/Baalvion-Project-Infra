'use client';

import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export function OfferForm() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Offer Form</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">This is a placeholder for the offer form.</p>
      </CardContent>
    </Card>
  )
}
