
// This file can be used to manage application routes and navigation items.
// For now, the sidebar configuration is handled in sidebar.config.ts

export const publicRoutes = [
    '/login',
    '/signup',
    '/forgot-password',
    '/us/careers',
    '/us/about',
    // etc.
];

export const authRoutes = ['/login', '/signup', '/forgot-password'];

export const adminRoutes = ['/dashboard', '/candidates', '/jobs'];
