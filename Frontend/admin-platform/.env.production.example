# ─────────────────────────────────────────────────────────────────────────────
# admin-platform — PRODUCTION env reference (admin.baalvion.com on Vercel)
#
# Copy these into Vercel → Project → Settings → Environment Variables (Production).
# All URLs route through the single public Traefik ingress (api.baalvion.com),
# per Backend/infra/api-gateway/dynamic.yml. Path convention:
#   https://api.baalvion.com/api/v1/<domain>/<service> + the service's mount path
#
# Do NOT copy .env.local here — those localhost values only work in dev.
# The core API vars below already have production-correct fallbacks baked into
# src/lib/api/client.ts; they are listed explicitly for clarity/overridability.
# ─────────────────────────────────────────────────────────────────────────────

# ── App identity ──
NEXT_PUBLIC_APP_URL=https://admin.baalvion.com
NEXT_PUBLIC_APP_ENV=production
NEXT_PUBLIC_APP_NAME=Baalvion Admin
NEXT_PUBLIC_ACCESS_TOKEN_TTL=900000

# ── Gateway + core identity / admin / CMS ──
NEXT_PUBLIC_GATEWAY_URL=https://api.baalvion.com
NEXT_PUBLIC_API_URL=https://api.baalvion.com/api/v1/infrastructure/proxy/v1
NEXT_PUBLIC_ADMIN_API_URL=https://api.baalvion.com/api/v1/platform/admin/v1
NEXT_PUBLIC_SESSION_API_URL=https://api.baalvion.com/api/v1/identity/session/v1
NEXT_PUBLIC_OAUTH_URL=https://api.baalvion.com/api/v1/identity/oauth
NEXT_PUBLIC_CMS_API_URL=https://api.baalvion.com/api/v1/knowledge/cms/api/v1

# ── Auth (server-side rewrite target in next.config.ts; default already equals this) ──
AUTH_PROXY_TARGET=https://api.baalvion.com/api/v1/identity/auth/v1/auth

# ── Realtime metrics (WebSocket). Verify the path against realtime-service once live. ──
NEXT_PUBLIC_WS_URL=wss://api.baalvion.com/api/v1/infrastructure/realtime

# ── Per-domain service clients ──────────────────────────────────────────────
# Each entry = gateway namespace (/api/v1/<domain>/<service>) + the service's
# own mount path (/api/v1 for most, /v1 for law/audit/notifications). Verified
# against dynamic.yml routers. `orders` is intentionally OMITTED — order-service
# is deprecated (lifecycle moved to the trade domain's order-execution-service);
# the admin `orders` client must be repointed there before those pages work.
# rbac / audit / crm were added to dynamic.yml alongside this file.
NEXT_PUBLIC_SERVICE_URLS={"jobs":"https://api.baalvion.com/api/v1/ecosystem/jobs/api/v1","mining":"https://api.baalvion.com/api/v1/ecosystem/mining/api/v1","imperialpedia":"https://api.baalvion.com/api/v1/knowledge/imperialpedia/api/v1","realEstate":"https://api.baalvion.com/api/v1/ecosystem/real-estate/api/v1","brand":"https://api.baalvion.com/api/v1/ecosystem/brand-connector/api/v1","market":"https://api.baalvion.com/api/v1/commerce/market/api/v1","ir":"https://api.baalvion.com/api/v1/ecosystem/ir/api/v1","dashboard":"https://api.baalvion.com/api/v1/platform/dashboard/api/v1","about":"https://api.baalvion.com/api/v1/ecosystem/about/api/v1","ctm":"https://api.baalvion.com/api/v1/ecosystem/ctm/api/v1","commerce":"https://api.baalvion.com/api/v1/commerce/commerce/api/v1","inventory":"https://api.baalvion.com/api/v1/commerce/inventory/api/v1","fulfillment":"https://api.baalvion.com/api/v1/commerce/fulfillment/api/v1","law":"https://api.baalvion.com/api/v1/knowledge/law/v1","notifications":"https://api.baalvion.com/api/v1/infrastructure/notification/v1","rbac":"https://api.baalvion.com/api/v1/identity/rbac/api/v1","audit":"https://api.baalvion.com/api/v1/infrastructure/audit/v1","crm":"https://api.baalvion.com/api/v1/ecosystem/crm/api/v1"}

# ── CRM (separate single-service var; routed via gateway as of this change) ──
NEXT_PUBLIC_CRM_API_URL=https://api.baalvion.com/api/v1/ecosystem/crm/api/v1
