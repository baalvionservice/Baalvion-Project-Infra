# Insiders backend (server-side fetch of curated public data). Dev → direct; prod → gateway/edge public route.
INSIDERS_API_URL=http://localhost:3050/v1
# Canonical public site origin (used for canonical URLs, sitemap, OG).
SITE_URL=https://insiders.baalvion.com
