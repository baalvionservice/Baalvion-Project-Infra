/**
 * Single toggle for the glossary/topic-discovery surface: the /terms hub, the
 * A-Z and 0-9 letter listing pages, individual term pages, /topics,
 * /learning-paths, and the homepage "Trending Topics" rail.
 *
 * Taken offline (2026-08-02) pending Google AdSense approval — this surface
 * is exactly the "thin/auto-generated content" pattern AdSense review flags.
 * Flip to `true` once AdSense approves and the underlying term content has
 * been built out for real.
 */
export const GLOSSARY_LIVE = false;
