# Committed intentionally: GA measurement IDs are public by design (they are
# embedded in every page's rendered HTML), not secrets. Next.js loads this
# file automatically on any build platform (Vercel, Cloudflare Workers
# Builds, CI, EC2/PM2) without needing a platform dashboard/token.
NEXT_PUBLIC_GA_ID=G-23R9XFBJGY
