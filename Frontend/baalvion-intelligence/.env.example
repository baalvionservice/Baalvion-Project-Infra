NEWS_SERVICE_URL=http://localhost:3045
NEWS_API_KEY=CHANGE_ME

# Real signup/login (auth-bff proxies to auth-service's /v1/auth/*, see
# src/app/auth-bff/[...path]/route.ts)
AUTH_SERVICE_URL=http://localhost:3001

# Real API key issuance + Razorpay checkout order creation (src/app/api/keys/*,
# src/app/api/billing/*, see src/lib/developer-api.server.ts). Razorpay keys themselves live only
# on developer-service — see Backend/services/infrastructure/developer-service/.env.example.
DEVELOPER_SERVICE_URL=http://localhost:3042
