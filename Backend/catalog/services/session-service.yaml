apiVersion: baalvion.io/v1
kind: Service
metadata:
  name: session-service
  description: TODO — describe this bounded context.
  domain: identity
  division: platform-core
  context: session
  owner: "@baalvion/platform-core"
  tier: tier-0
  repo: baalvion-platform
  path: Backend/services/identity/session-service
spec:
  lifecycle: production
  language: node
  datastores: [redis, postgres]
  dependsOn: [auth-service]
  consumesEvents: []
  producesEvents: []
  apis: ["/v1/session"]
  slo: { availability: 0.99, latencyP95Ms: 400 }
  deploy: { chart: baalvion-service, namespace: baalvion-identity, minReplicas: 2, maxReplicas: 10 }
