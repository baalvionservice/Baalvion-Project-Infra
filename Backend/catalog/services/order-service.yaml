apiVersion: baalvion.io/v1
kind: Service
metadata:
  name: order-service
  description: TODO — describe this bounded context.
  domain: commerce
  division: commerce
  context: order
  owner: "@baalvion/commerce"
  tier: tier-1
  repo: baalvion-platform
  path: Backend/services/commerce/order-service
spec:
  lifecycle: production
  language: node
  datastores: [postgres]
  dependsOn: [identity-platform]
  consumesEvents: []
  producesEvents: []
  apis: ["/v1/order"]
  slo: { availability: 0.99, latencyP95Ms: 400 }
  deploy: { chart: baalvion-service, namespace: baalvion-commerce, minReplicas: 2, maxReplicas: 10 }
