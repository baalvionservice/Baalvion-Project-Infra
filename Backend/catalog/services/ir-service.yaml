apiVersion: baalvion.io/v1
kind: Service
metadata:
  name: ir-service
  description: TODO — describe this bounded context.
  domain: ecosystem
  division: commerce
  context: investor-relations
  owner: "@baalvion/ecosystem"
  tier: tier-2
  repo: baalvion-platform
  path: Backend/services/ecosystem/ir-service
spec:
  lifecycle: production
  language: node
  datastores: [postgres]
  dependsOn: [auth-service]
  consumesEvents: []
  producesEvents: []
  apis: ["/v1/investor-relations"]
  slo: { availability: 0.99, latencyP95Ms: 400 }
  deploy: { chart: baalvion-service, namespace: baalvion-ecosystem, minReplicas: 2, maxReplicas: 10 }
