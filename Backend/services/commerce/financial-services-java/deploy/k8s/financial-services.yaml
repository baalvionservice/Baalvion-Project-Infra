# Kubernetes manifests for the Baalvion financial services (design §8.2 / §9.2).
# namespace + shared config + Deployment/Service/HPA per service.
# Images are expected as baalvion/<service>:latest (built from the shared Dockerfile).
# Secrets (DB password, etc.) should come from a Secret/External Secrets Operator in prod;
# inline values here are for non-prod only.
---
apiVersion: v1
kind: Namespace
metadata:
  name: financial
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: financial-config
  namespace: financial
data:
  DB_HOST: "postgres"
  DB_PORT: "5432"
  DB_NAME: "baalvion"
  DB_USER: "postgres"
  KAFKA_BROKERS: "kafka:9092"
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"
  # Zero-trust: enforce JWT and point the resource server at the platform RS256 JWKS.
  APP_SECURITY_ENABLED: "true"
  OAUTH_ISSUER_URI: "https://auth.baalvion.com"
  OAUTH_JWK_SET_URI: "http://oauth-service.default.svc.cluster.local:3023/.well-known/jwks.json"
---
apiVersion: v1
kind: Secret
metadata:
  name: financial-secrets
  namespace: financial
type: Opaque
stringData:
  DB_PASSWORD: "postgres"   # replace via External Secrets Operator in prod
---
# ---------- account-service (3016) ----------
apiVersion: apps/v1
kind: Deployment
metadata: { name: account-service, namespace: financial }
spec:
  replicas: 2
  selector: { matchLabels: { app: account-service } }
  template:
    metadata: { labels: { app: account-service } }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: account-service
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
            seccompProfile: { type: RuntimeDefault }
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          image: baalvion/account-service:latest
          ports: [{ containerPort: 3016 }]
          envFrom:
            - configMapRef: { name: financial-config }
            - secretRef: { name: financial-secrets }
          env: [{ name: PORT, value: "3016" }]
          readinessProbe: { httpGet: { path: /actuator/health/readiness, port: 3016 }, initialDelaySeconds: 30, periodSeconds: 10 }
          livenessProbe:  { httpGet: { path: /actuator/health/liveness,  port: 3016 }, initialDelaySeconds: 45, periodSeconds: 15 }
          resources: { requests: { cpu: "250m", memory: "512Mi" }, limits: { cpu: "1", memory: "1Gi" } }
---
apiVersion: v1
kind: Service
metadata: { name: account-service, namespace: financial }
spec:
  selector: { app: account-service }
  ports: [{ port: 3016, targetPort: 3016 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: account-service, namespace: financial }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: account-service }
  minReplicas: 2
  maxReplicas: 10
  metrics: [{ type: Resource, resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } } }]
---
# ---------- ledger-service (3014) ----------
apiVersion: apps/v1
kind: Deployment
metadata: { name: ledger-service, namespace: financial }
spec:
  replicas: 2
  selector: { matchLabels: { app: ledger-service } }
  template:
    metadata: { labels: { app: ledger-service } }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: ledger-service
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
            seccompProfile: { type: RuntimeDefault }
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          image: baalvion/ledger-service:latest
          ports: [{ containerPort: 3014 }]
          envFrom:
            - configMapRef: { name: financial-config }
            - secretRef: { name: financial-secrets }
          env: [{ name: PORT, value: "3014" }]
          readinessProbe: { httpGet: { path: /actuator/health/readiness, port: 3014 }, initialDelaySeconds: 30, periodSeconds: 10 }
          livenessProbe:  { httpGet: { path: /actuator/health/liveness,  port: 3014 }, initialDelaySeconds: 45, periodSeconds: 15 }
          resources: { requests: { cpu: "250m", memory: "512Mi" }, limits: { cpu: "1", memory: "1Gi" } }
---
apiVersion: v1
kind: Service
metadata: { name: ledger-service, namespace: financial }
spec:
  selector: { app: ledger-service }
  ports: [{ port: 3014, targetPort: 3014 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: ledger-service, namespace: financial }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: ledger-service }
  minReplicas: 2
  maxReplicas: 10
  metrics: [{ type: Resource, resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } } }]
---
# ---------- payment-service (3015) ----------
apiVersion: apps/v1
kind: Deployment
metadata: { name: payment-service, namespace: financial }
spec:
  replicas: 2
  selector: { matchLabels: { app: payment-service } }
  template:
    metadata: { labels: { app: payment-service } }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: payment-service
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
            seccompProfile: { type: RuntimeDefault }
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          image: baalvion/payment-service:latest
          ports: [{ containerPort: 3015 }]
          envFrom:
            - configMapRef: { name: financial-config }
            - secretRef: { name: financial-secrets }
          env: [{ name: PORT, value: "3015" }]
          readinessProbe: { httpGet: { path: /actuator/health/readiness, port: 3015 }, initialDelaySeconds: 30, periodSeconds: 10 }
          livenessProbe:  { httpGet: { path: /actuator/health/liveness,  port: 3015 }, initialDelaySeconds: 45, periodSeconds: 15 }
          resources: { requests: { cpu: "500m", memory: "512Mi" }, limits: { cpu: "1", memory: "1Gi" } }
---
apiVersion: v1
kind: Service
metadata: { name: payment-service, namespace: financial }
spec:
  selector: { app: payment-service }
  ports: [{ port: 3015, targetPort: 3015 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: payment-service, namespace: financial }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: payment-service }
  minReplicas: 2
  maxReplicas: 10
  metrics: [{ type: Resource, resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } } }]
---
# ---------- escrow-service (3017) ----------
apiVersion: apps/v1
kind: Deployment
metadata: { name: escrow-service, namespace: financial }
spec:
  replicas: 2
  selector: { matchLabels: { app: escrow-service } }
  template:
    metadata: { labels: { app: escrow-service } }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: escrow-service
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
            seccompProfile: { type: RuntimeDefault }
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          image: baalvion/escrow-service:latest
          ports: [{ containerPort: 3017 }]
          envFrom:
            - configMapRef: { name: financial-config }
            - secretRef: { name: financial-secrets }
          env: [{ name: PORT, value: "3017" }]
          readinessProbe: { httpGet: { path: /actuator/health/readiness, port: 3017 }, initialDelaySeconds: 30, periodSeconds: 10 }
          livenessProbe:  { httpGet: { path: /actuator/health/liveness,  port: 3017 }, initialDelaySeconds: 45, periodSeconds: 15 }
          resources: { requests: { cpu: "250m", memory: "512Mi" }, limits: { cpu: "1", memory: "1Gi" } }
---
apiVersion: v1
kind: Service
metadata: { name: escrow-service, namespace: financial }
spec:
  selector: { app: escrow-service }
  ports: [{ port: 3017, targetPort: 3017 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: escrow-service, namespace: financial }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: escrow-service }
  minReplicas: 2
  maxReplicas: 10
  metrics: [{ type: Resource, resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } } }]
---
# ---------- settlement-service (3018) ----------
apiVersion: apps/v1
kind: Deployment
metadata: { name: settlement-service, namespace: financial }
spec:
  replicas: 2
  selector: { matchLabels: { app: settlement-service } }
  template:
    metadata: { labels: { app: settlement-service } }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: settlement-service
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
            seccompProfile: { type: RuntimeDefault }
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          image: baalvion/settlement-service:latest
          ports: [{ containerPort: 3018 }]
          envFrom:
            - configMapRef: { name: financial-config }
            - secretRef: { name: financial-secrets }
          env: [{ name: PORT, value: "3018" }]
          readinessProbe: { httpGet: { path: /actuator/health/readiness, port: 3018 }, initialDelaySeconds: 30, periodSeconds: 10 }
          livenessProbe:  { httpGet: { path: /actuator/health/liveness,  port: 3018 }, initialDelaySeconds: 45, periodSeconds: 15 }
          resources: { requests: { cpu: "250m", memory: "512Mi" }, limits: { cpu: "1", memory: "1Gi" } }
---
apiVersion: v1
kind: Service
metadata: { name: settlement-service, namespace: financial }
spec:
  selector: { app: settlement-service }
  ports: [{ port: 3018, targetPort: 3018 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: settlement-service, namespace: financial }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: settlement-service }
  minReplicas: 2
  maxReplicas: 10
  metrics: [{ type: Resource, resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } } }]
---
# ---------- reconciliation-service (3019) ----------
apiVersion: apps/v1
kind: Deployment
metadata: { name: reconciliation-service, namespace: financial }
spec:
  replicas: 2
  selector: { matchLabels: { app: reconciliation-service } }
  template:
    metadata: { labels: { app: reconciliation-service } }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: reconciliation-service
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
            seccompProfile: { type: RuntimeDefault }
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          image: baalvion/reconciliation-service:latest
          ports: [{ containerPort: 3019 }]
          envFrom:
            - configMapRef: { name: financial-config }
            - secretRef: { name: financial-secrets }
          env: [{ name: PORT, value: "3019" }]
          readinessProbe: { httpGet: { path: /actuator/health/readiness, port: 3019 }, initialDelaySeconds: 30, periodSeconds: 10 }
          livenessProbe:  { httpGet: { path: /actuator/health/liveness,  port: 3019 }, initialDelaySeconds: 45, periodSeconds: 15 }
          resources: { requests: { cpu: "250m", memory: "512Mi" }, limits: { cpu: "1", memory: "1Gi" } }
---
apiVersion: v1
kind: Service
metadata: { name: reconciliation-service, namespace: financial }
spec:
  selector: { app: reconciliation-service }
  ports: [{ port: 3019, targetPort: 3019 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: reconciliation-service, namespace: financial }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: reconciliation-service }
  minReplicas: 2
  maxReplicas: 10
  metrics: [{ type: Resource, resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } } }]
---
# ---------- audit-service (3020) ----------
apiVersion: apps/v1
kind: Deployment
metadata: { name: audit-service, namespace: financial }
spec:
  replicas: 2
  selector: { matchLabels: { app: audit-service } }
  template:
    metadata: { labels: { app: audit-service } }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: audit-service
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
            seccompProfile: { type: RuntimeDefault }
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          image: baalvion/audit-service:latest
          ports: [{ containerPort: 3020 }]
          envFrom:
            - configMapRef: { name: financial-config }
            - secretRef: { name: financial-secrets }
          env: [{ name: PORT, value: "3020" }]
          readinessProbe: { httpGet: { path: /actuator/health/readiness, port: 3020 }, initialDelaySeconds: 30, periodSeconds: 10 }
          livenessProbe:  { httpGet: { path: /actuator/health/liveness,  port: 3020 }, initialDelaySeconds: 45, periodSeconds: 15 }
          resources: { requests: { cpu: "250m", memory: "512Mi" }, limits: { cpu: "1", memory: "1Gi" } }
---
apiVersion: v1
kind: Service
metadata: { name: audit-service, namespace: financial }
spec:
  selector: { app: audit-service }
  ports: [{ port: 3020, targetPort: 3020 }]
---
# ---------- reporting-service (3024) ----------
apiVersion: apps/v1
kind: Deployment
metadata: { name: reporting-service, namespace: financial }
spec:
  replicas: 2
  selector: { matchLabels: { app: reporting-service } }
  template:
    metadata: { labels: { app: reporting-service } }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: reporting-service
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
            seccompProfile: { type: RuntimeDefault }
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          image: baalvion/reporting-service:latest
          ports: [{ containerPort: 3024 }]
          envFrom:
            - configMapRef: { name: financial-config }
            - secretRef: { name: financial-secrets }
          env: [{ name: PORT, value: "3024" }]
          readinessProbe: { httpGet: { path: /actuator/health/readiness, port: 3024 }, initialDelaySeconds: 30, periodSeconds: 10 }
          livenessProbe:  { httpGet: { path: /actuator/health/liveness,  port: 3024 }, initialDelaySeconds: 45, periodSeconds: 15 }
          resources: { requests: { cpu: "250m", memory: "512Mi" }, limits: { cpu: "1", memory: "1Gi" } }
---
apiVersion: v1
kind: Service
metadata: { name: reporting-service, namespace: financial }
spec:
  selector: { app: reporting-service }
  ports: [{ port: 3024, targetPort: 3024 }]
---
# ---------- risk-service (3035) ----------
apiVersion: apps/v1
kind: Deployment
metadata: { name: risk-service, namespace: financial }
spec:
  replicas: 2
  selector: { matchLabels: { app: risk-service } }
  template:
    metadata: { labels: { app: risk-service } }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: risk-service
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
            seccompProfile: { type: RuntimeDefault }
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          image: baalvion/risk-service:latest
          ports: [{ containerPort: 3035 }]
          envFrom:
            - configMapRef: { name: financial-config }
            - secretRef: { name: financial-secrets }
          env: [{ name: PORT, value: "3035" }]
          readinessProbe: { httpGet: { path: /actuator/health/readiness, port: 3035 }, initialDelaySeconds: 30, periodSeconds: 10 }
          livenessProbe:  { httpGet: { path: /actuator/health/liveness,  port: 3035 }, initialDelaySeconds: 45, periodSeconds: 15 }
          resources: { requests: { cpu: "250m", memory: "512Mi" }, limits: { cpu: "1", memory: "1Gi" } }
---
apiVersion: v1
kind: Service
metadata: { name: risk-service, namespace: financial }
spec:
  selector: { app: risk-service }
  ports: [{ port: 3035, targetPort: 3035 }]
---
# ---------- trade-finance-service (3036) ----------
apiVersion: apps/v1
kind: Deployment
metadata: { name: trade-finance-service, namespace: financial }
spec:
  replicas: 2
  selector: { matchLabels: { app: trade-finance-service } }
  template:
    metadata: { labels: { app: trade-finance-service } }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: trade-finance-service
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
            seccompProfile: { type: RuntimeDefault }
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          image: baalvion/trade-finance-service:latest
          ports: [{ containerPort: 3036 }]
          envFrom:
            - configMapRef: { name: financial-config }
            - secretRef: { name: financial-secrets }
          env: [{ name: PORT, value: "3036" }]
          readinessProbe: { httpGet: { path: /actuator/health/readiness, port: 3036 }, initialDelaySeconds: 30, periodSeconds: 10 }
          livenessProbe:  { httpGet: { path: /actuator/health/liveness,  port: 3036 }, initialDelaySeconds: 45, periodSeconds: 15 }
          resources: { requests: { cpu: "250m", memory: "512Mi" }, limits: { cpu: "1", memory: "1Gi" } }
---
apiVersion: v1
kind: Service
metadata: { name: trade-finance-service, namespace: financial }
spec:
  selector: { app: trade-finance-service }
  ports: [{ port: 3036, targetPort: 3036 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: trade-finance-service, namespace: financial }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: trade-finance-service }
  minReplicas: 2
  maxReplicas: 10
  metrics: [{ type: Resource, resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } } }]
---
# ---------- credit-service (3037) ----------
apiVersion: apps/v1
kind: Deployment
metadata: { name: credit-service, namespace: financial }
spec:
  replicas: 2
  selector: { matchLabels: { app: credit-service } }
  template:
    metadata: { labels: { app: credit-service } }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: credit-service
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
            seccompProfile: { type: RuntimeDefault }
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          image: baalvion/credit-service:latest
          ports: [{ containerPort: 3037 }]
          envFrom:
            - configMapRef: { name: financial-config }
            - secretRef: { name: financial-secrets }
          env: [{ name: PORT, value: "3037" }]
          readinessProbe: { httpGet: { path: /actuator/health/readiness, port: 3037 }, initialDelaySeconds: 30, periodSeconds: 10 }
          livenessProbe:  { httpGet: { path: /actuator/health/liveness,  port: 3037 }, initialDelaySeconds: 45, periodSeconds: 15 }
          resources: { requests: { cpu: "250m", memory: "512Mi" }, limits: { cpu: "1", memory: "1Gi" } }
---
apiVersion: v1
kind: Service
metadata: { name: credit-service, namespace: financial }
spec:
  selector: { app: credit-service }
  ports: [{ port: 3037, targetPort: 3037 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: credit-service, namespace: financial }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: credit-service }
  minReplicas: 2
  maxReplicas: 10
  metrics: [{ type: Resource, resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } } }]
---
# ---------- fx-service (3038) ----------
apiVersion: apps/v1
kind: Deployment
metadata: { name: fx-service, namespace: financial }
spec:
  replicas: 2
  selector: { matchLabels: { app: fx-service } }
  template:
    metadata: { labels: { app: fx-service } }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: fx-service
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
            seccompProfile: { type: RuntimeDefault }
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          image: baalvion/fx-service:latest
          ports: [{ containerPort: 3038 }]
          envFrom:
            - configMapRef: { name: financial-config }
            - secretRef: { name: financial-secrets }
          env: [{ name: PORT, value: "3038" }]
          readinessProbe: { httpGet: { path: /actuator/health/readiness, port: 3038 }, initialDelaySeconds: 30, periodSeconds: 10 }
          livenessProbe:  { httpGet: { path: /actuator/health/liveness,  port: 3038 }, initialDelaySeconds: 45, periodSeconds: 15 }
          resources: { requests: { cpu: "250m", memory: "512Mi" }, limits: { cpu: "1", memory: "1Gi" } }
---
apiVersion: v1
kind: Service
metadata: { name: fx-service, namespace: financial }
spec:
  selector: { app: fx-service }
  ports: [{ port: 3038, targetPort: 3038 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: fx-service, namespace: financial }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: fx-service }
  minReplicas: 2
  maxReplicas: 10
  metrics: [{ type: Resource, resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } } }]
---
# ---------- wallet-service (3039) ----------
apiVersion: apps/v1
kind: Deployment
metadata: { name: wallet-service, namespace: financial }
spec:
  replicas: 2
  selector: { matchLabels: { app: wallet-service } }
  template:
    metadata: { labels: { app: wallet-service } }
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile: { type: RuntimeDefault }
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: wallet-service
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
            seccompProfile: { type: RuntimeDefault }
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          image: baalvion/wallet-service:latest
          ports: [{ containerPort: 3039 }]
          envFrom:
            - configMapRef: { name: financial-config }
            - secretRef: { name: financial-secrets }
          env: [{ name: PORT, value: "3039" }]
          readinessProbe: { httpGet: { path: /actuator/health/readiness, port: 3039 }, initialDelaySeconds: 30, periodSeconds: 10 }
          livenessProbe:  { httpGet: { path: /actuator/health/liveness,  port: 3039 }, initialDelaySeconds: 45, periodSeconds: 15 }
          resources: { requests: { cpu: "250m", memory: "512Mi" }, limits: { cpu: "1", memory: "1Gi" } }
---
apiVersion: v1
kind: Service
metadata: { name: wallet-service, namespace: financial }
spec:
  selector: { app: wallet-service }
  ports: [{ port: 3039, targetPort: 3039 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: wallet-service, namespace: financial }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: wallet-service }
  minReplicas: 2
  maxReplicas: 10
  metrics: [{ type: Resource, resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } } }]
