{
    "_comment": "Local dev fallback ONLY — never put real/cloud credentials here. Real connections come from DB_HOST/DB_PORT/DB_NAME/DB_USER/DB_PASSWORD env vars (see models/index.js), which take precedence over this block. A live Neon credential previously committed here was removed; it must be rotated on the provider side as it remains in git history.",
    "development": {
        "username": "baalvion",
        "password": "",
        "database": "baalvion_db",
        "host": "127.0.0.1",
        "port": 5432,
        "dialect": "postgres"
    }
}