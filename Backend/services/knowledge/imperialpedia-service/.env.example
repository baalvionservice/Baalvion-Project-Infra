PORT=3004
NODE_ENV=development
JWT_ACCESS_SECRET=replace-with-strong-secret
DB_HOST=localhost
DB_PORT=5432
DB_NAME=baalvion_db
DB_USER=baalvion
DB_PASSWORD=
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ─── Payments (Razorpay) ────────────────────────────────────────────────────
# Preferred: paste the live key in the admin panel's "Integrations & Keys" vault for the
# `imperialpedia` site (category=payment, provider=razorpay) — no redeploy needed, checkout picks
# it up within 60s. These env vars are ONLY a local-dev fallback when the vault is unreachable.
PAYMENT_SITE_SLUG=imperialpedia
CMS_BASE_URL=
INTERNAL_SERVICE_SECRET=
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=
