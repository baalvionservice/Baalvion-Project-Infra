{
  "name": "@baalvion/graceful-shutdown",
  "version": "1.0.0",
  "description": "Graceful HTTP server shutdown with registered cleanup handlers (committed plain CommonJS — no build step, resolves in pruned production images)",
  "private": true,
  "main": "./index.js",
  "types": "./index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "default": "./index.js"
    },
    "./package.json": "./package.json"
  },
  "files": ["index.js", "index.d.ts"],
  "scripts": {
    "build": "node -e \"process.exit(0)\"",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {},
  "devDependencies": {}
}
