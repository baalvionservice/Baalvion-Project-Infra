export { normalizeEmail, normalizePhone, normalizeName, PartyError } from './normalize';
export { matchKeys, resolveParty, planMerge } from './resolve';
export type {
  IdentitySignal,
  MatchKey,
  PartyCandidate,
  Resolution,
  ResolutionOutcome,
  SignalStrength,
  MergePlan,
} from './resolve';
