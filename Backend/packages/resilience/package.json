{
  "name": "@baalvion/resilience",
  "version": "1.0.0",
  "description": "Transport-agnostic resilience primitives: circuit breaker, retry/backoff, timeout, bulkhead, and a distributed (Redis) rate limiter. Wrap ANY async operation — DB, vendor SDK, queue publish, HTTP.",
  "private": true,
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.d.ts",
      "require": "./src/index.js",
      "import": "./src/index.js"
    }
  },
  "files": ["src"],
  "scripts": {
    "test": "node --test",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@types/node": "^22.0.0"
  }
}
