{
  "name": "@baalvion/service-kit",
  "version": "1.0.0",
  "description": "One-call service bootstrap — composes telemetry, identity, RBAC, the event bus, health/metrics endpoints and graceful shutdown into a production-ready Express service.",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.mjs", "require": "./dist/index.js" }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@baalvion/events": "workspace:*",
    "@baalvion/telemetry": "workspace:*",
    "@baalvion/graceful-shutdown": "workspace:*",
    "@baalvion/rbac": "workspace:*",
    "@baalvion/types": "workspace:*"
  },
  "peerDependencies": {
    "express": "^4.19.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.7.2",
    "rimraf": "^6.0.1",
    "@types/express": "^4.17.21"
  }
}
