{
  "name": "@baalvion/entitlements",
  "engines": {
    "node": ">=24",
    "pnpm": ">=9"
  },
  "version": "1.0.0",
  "description": "Entitlement decisions \u2014 the per-site premium-content rule (decideAccess) plus cross-site grants keyed on the group-wide partyId, so paying on one property can unlock another.",
  "private": true,
  "main": "index.js",
  "files": [
    "index.js",
    "grants.js"
  ],
  "scripts": {
    "test": "node --test"
  }
}
