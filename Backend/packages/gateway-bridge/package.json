{
  "name": "@baalvion/gateway-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Verify auth-gateway injected identity (signed v1 headers + v2 envelope).",
  "main": "index.js",
  "license": "ISC",
  "scripts": {
    "test": "node --test"
  }
}
