{
  "name": "@baalvion/notify-ntfy",
  "version": "1.0.0",
  "private": true,
  "description": "ntfy publisher + dead-man's-switch heartbeat — the platform's phone-reaching alert channel, usable without notification-service so an alert still lands when notification-service is itself down.",
  "main": "index.js",
  "files": ["index.js"],
  "scripts": {
    "test": "node --test test/*.test.mjs"
  }
}
