# BAALVION OMEGA-INFINITY OS: PLANETARY RUNTIME TOPOLOGY
# Deployment Strategy: Multi-Cloud / Sovereign Regional Hypergrid
# Security Level: INFINITY (mTLS / SPIFFE / Kyverno / Zero-Trust)

apiVersion: apps/v1
kind: Deployment
metadata:
  name: baalvion-infinity-coordinator
  namespace: infinity-core
spec:
  replicas: 124 # Planetary distribution across all 124 hyper-clusters
  selector:
    matchLabels:
      app: infinity-coordinator
  template:
    metadata:
      labels:
        app: infinity-coordinator
        security.baalvion.gov/mtls: "true"
        tier: supreme-execution
        domain: global-kernel
    spec:
      containers:
      - name: coordinator-runtime
        image: vault.baalvion.gov/core/infinity-coordinator:v9.0.0-FINAL
        ports:
        - containerPort: 3000
        envFrom:
        - secretRef:
            name: infinity-authority-secrets
        resources:
          limits:
            cpu: "4"
            memory: "8Gi"
          requests:
            cpu: "2"
            memory: "4Gi"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
        readinessProbe:
          httpGet:
            path: /ready
            port: 3000

---
# SOVEREIGN ISOLATION POLICY (Kyverno)
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: enforce-infinity-isolation
spec:
  validationFailureAction: Enforce
  background: true
  rules:
  - name: require-sovereign-mtls
    match:
      any:
      - resources:
          kinds:
          - Pod
    validate:
      message: "Nodes must utilize the Infinity mTLS fabric (SPIFFE) for institutional cross-talk."
      pattern:
        metadata:
          labels:
            security.baalvion.gov/mtls: "true"
  - name: pin-data-jurisdiction
    match:
      any:
      - resources:
          kinds:
          - PersistentVolumeClaim
    validate:
      message: "Data residency violation: Persistent storage must be bound to the jurisdictional regional cluster."
      pattern:
        metadata:
          labels:
            residency: "{{region}}"